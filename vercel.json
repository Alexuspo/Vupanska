{
    "buildCommand": "mkdocs build",
    "outputDirectory": "site",
    "installCommand": "pip install -r requirements.txt",
    "framework": "python",
    "runtime": {
        "pythonVersion": "3.9"
    }
}
