{
    "version": 2,
    "builds": [{
        "src": "requirements.txt",
        "use": "@vercel/python"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/"
    }],
    "buildCommand": "python -m mkdocs build",
    "outputDirectory": "site"
}
